File aur Directory Commands

ls → Files aur directories list karta hai
ls -la → Hidden files aur details ke sath list
cd → Directory change karta hai
pwd → Current working directory dikhata hai
mkdir → Nayi directory banata hai
rm → File delete karta hai
rm -r → Directory delete karta hai
cp → File copy karta hai
mv → File move/rename karta hai

Examples:
ls -la
cd /home/user
mkdir test
cp file1 file2
mv old.txt new.txt

---
