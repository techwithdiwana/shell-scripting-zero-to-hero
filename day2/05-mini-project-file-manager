Mini Project: File Manager Script

#!/bin/bash
# Simple File Manager Script

echo "Choose an option: list/create/delete"
read option

if [ "$option" == "list" ]; then
    ls -la
elif [ "$option" == "create" ]; then
    read -p "Enter filename: " fname
    touch $fname
    echo "$fname created"
elif [ "$option" == "delete" ]; then
    read -p "Enter filename: " fname
    rm -i $fname
    echo "$fname deleted"
else
    echo "Invalid option"
fi

Run:
./file-manager.sh

---
